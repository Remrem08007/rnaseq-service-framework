[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rnaseq-service-framework"
version = "0.1.0.dev0"
description = "Client-delivery validation and orchestration for reproducible RNA-seq analysis"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
  {name = "Rémi-Antoine Joron"},
]
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8",
]

[project.scripts]
rnaseq-service-preflight = "rnaseq_service.cli:main"
rnaseq-service-plan = "rnaseq_service.plan_cli:main"
rnaseq-service-bundle = "rnaseq_service.bundle_cli:main"
rnaseq-service-stage = "rnaseq_service.staging_cli:main"
rnaseq-service-hpc = "rnaseq_service.hpc_cli:main"
rnaseq-service-primary = "rnaseq_service.primary_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
